## Lab 3-Task1

I create a registration form in React. I use useState for name, email and age. Every input have value and onChange to update state. I also create error states but not use them yet. I add a submit button and onSubmit that prevent default page reload.

I learned how React events work, how to use controlled inputs and update state. I understand event.preventDefault() stop page reload. Error state is ready for next tasks.